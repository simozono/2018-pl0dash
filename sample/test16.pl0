const n = 2;
var a;

function func2(x, y)
  var z;
  begin
    if x < 2 then
    begin
      return 0;
    end;
    z := 1 + func2(x-1, y-1);
    write z; writeln;
    return z;
  end;

begin
  a := n - 1;
  write 1+func2(n+1, a+1);
  writeln;
end.
